

import <bool>
import <show>

@R "**" 5


x ** y = (x, y);

import <StdEnv>


dingus x y = (x, y);


lmap f () = ();
lmap f (a, rest) = (f a, lmap f rest);
@L ">>=" 1

main = do (>>=) {
    blah <- getLine;
    putStrLn blah;
    };

foo = (1, (2, (3, (4, ()))));