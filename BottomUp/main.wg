

import <bool>
import <show>

@R "**" 5


x ** y = (x, y);

import <StdEnv>


dingus x y = (x, y);


lmap f () = ();
lmap f (a, rest) = (f a, lmap f rest);


main = putStrLn (toStr (lmap (dingus 6) foo));

foo = (1, (2, (3, (4, ()))));