

import <bool>
import <show>

@R "**" 5


x ** y = (x, y);

import <StdEnv>


dingus x y = (x, y);


lmap f () = ();
lmap f (a, rest) = (f a, lmap f rest);
@L ">>=" 1

main = do (>>=) {
    fhand <- openRead "crap.txt";
    blah <- hReadLn fhand;
    putStrLn blah;
    //apples <- getContents fhand;
    //putStrLn apples;
    putStrLn "fuck";
    hClose fhand;
    };

foo = (1, (2, (3, (4, ()))));